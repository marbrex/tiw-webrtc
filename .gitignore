dist
.DS_Store
/*.env

########## YARN ##########
# Not using Yarn Zero-Installs, so ignore the cache
# But using it to generate Docker images, so keep the cache in .dockerignore
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

########## OBSIDIAN ##########
.obsidian/*
!.obsidian/app.json

.parcel-cache
